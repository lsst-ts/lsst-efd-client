### New features

- Use `lsst.rsp` for InfluxDB service discovery and credential retrieval if it is available.
- Fall back on an `EFDAUTH` environment variable to find InfluxDB credentials before attempting to use the Segwarides web service.
